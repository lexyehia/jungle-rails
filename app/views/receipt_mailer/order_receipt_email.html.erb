<h1>Thank you for making your recent Order #<%= @order.id %>, <%= @order.email %></h1>
<p>
Here is your order details:<br>
</p>
<p>
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Product</th>
        <th>Unit Price</th>
        <th>Quantity</th>
        <th>Total Price</th>
      </tr>
    </thead>
    <tbody>
      <% @order.line_items.each do |item| %>
      <tr>
        <td>
          <h4><%= item.product.name %></h4>
          <p><%= item.product.description %></p>
          <br>

        </td>
        <td>
          <%= humanized_money_with_symbol item.item_price %>
        </td>
        <td>
          <%= item.quantity %>
        </td>
        <td>
          <%= humanized_money_with_symbol item.total_price %>
        </td>
      </tr>
      <% end %>
    </tbody>
    <tfoot>
      <tr>
        <th colspan="3">
          TOTAL:
        </th>
        <th>
          <%= humanized_money_with_symbol @order.total %>
        </th>
      </tr>
    </tfoot>
  </table>
  </p>
  <p>
    HST No. 1234567879 RT0001. This Order is subject to our Terms of Service and Standard Shipping Policy.
  </p>
